// ------------------------------
// 가격 비교 에러 코드
// ------------------------------
// 전부 HTTP 에러 응답(4xx·5xx)의 `code`다. 서버 안내대로 실패 분기는 문구가 아니라 이 코드로 한다.
// 2026-09-17 dev 실측: job이 FAILED 상태로 끝나는 응답에는 코드·문구 필드가 없고,
// 원본 페이지 로드 실패(50204)는 job 생성 요청 자체가 502로 거절되는 형태로 온다.

export const COMPARE_REQUEST_ERROR_CODE = {
  /** 404 — 없는 jobId. 만료됐거나 조작된 값 (실측) */
  JOB_NOT_FOUND: 40428,
  /** 404 — 존재하지 않는 프리셋 (실측) */
  PRESET_NOT_FOUND: 40430,
  /**
   * 404 — "지원하지 않는 URL입니다."
   * 서버 공통 코드라 존재하지 않는 API 경로를 불러도 같은 코드가 온다. 쇼핑몰 미지원 전용 코드인지는 미확인
   */
  UNSUPPORTED_URL: 40400,
  /** 400 — 유효하지 않은 상품 URL (2026-08-27 서버 명세) */
  INVALID_URL: 40033,
  /** 400 — 접근이 허용되지 않은 주소 (2026-08-27 서버 명세) */
  FORBIDDEN_URL: 40034,
  /** 상품 페이지에서 정보를 추출하지 못함 (2026-08-27 서버 명세) */
  EXTRACT_FAILED: 40035,
  /** 502 — 상품 페이지를 불러오지 못함. job 생성 시점에 거절된다 (실측). 재시도하면 될 수 있는 실패 */
  PAGE_LOAD_FAILED: 50204,
  /** 비교 처리 시간 초과 (2026-08-27 서버 명세) */
  TIMEOUT: 50025,
} as const;
